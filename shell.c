#include "main.h"
/**
 * main - entry into program
 * @ac: cm arg count
 * @av: cm args
 * @env: env arg
 *  Return: Always 0
 */
int main(int ac, char **av, char **env)
{
	(void)ac;
	(void)av;

	prompt(env);

return (0);
}
